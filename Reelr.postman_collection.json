{
  "info": {
    "name": "Reelr API",
    "description": "Endpoints for MovieController (TMDB proxy).",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Get Movie",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/api/Movie/{{tmdbId}}",
          "host": ["{{baseUrl}}"],
          "path": ["api", "Movie", "{{tmdbId}}"]
        },
        "description": "Fetches a movie by TMDB id."
      }
    },
    {
      "name": "Search Movies",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/api/Movie/search?query={{searchQuery}}",
          "host": ["{{baseUrl}}"],
          "path": ["api", "Movie", "search"],
          "query": [
            {
              "key": "query",
              "value": "{{searchQuery}}"
            }
          ]
        },
        "description": "Searches movies by title."
      }
    },
    {
      "name": "Get Movie Credits",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/api/Movie/{{tmdbId}}/credits",
          "host": ["{{baseUrl}}"],
          "path": ["api", "Movie", "{{tmdbId}}", "credits"]
        },
        "description": "Fetches cast/crew for a movie."
      }
    },
    {
      "name": "Get Movie Details",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/api/Movie/{{tmdbId}}/details",
          "host": ["{{baseUrl}}"],
          "path": ["api", "Movie", "{{tmdbId}}", "details"]
        },
        "description": "Fetches detailed movie info."
      }
    }
  ]
}
